#[macro_use] extern crate log;
extern crate bytes;
extern crate env_logger;
extern crate httparse;
extern crate mio;
extern crate sthttp;

fn main() {
    //env_logger::init().unwrap();

    //let client = sthttp::client::HttpClient::new();

    //let google = client.connect("http://www.google.com");
    //let req = sthttp::request::Request::new()
    //google.send(&req);
}
